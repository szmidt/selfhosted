apiVersion: apps/v1
kind: Deployment
metadata:
  name: sftp-server
  namespace: media
  labels:
    app: sftp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sftp-server
  template:
    metadata:
      labels:
        app: sftp-server
    spec:
      securityContext:
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
      - name: sftp-server
        image: atmoz/sftp:latest
        ports:
        - containerPort: 22
        envFrom:
        - secretRef:
            name: sftp-user-credentials
        volumeMounts:
        - name: shared-media
          mountPath: /home/olaf/media
        - name: navidrome-music # deprected - use shared-media in the future
          mountPath: /home/olaf/music
      volumes:
      - name: shared-media
        persistentVolumeClaim:
          claimName: shared-media
      - name: navidrome-music # deprected - use shared-media in the future
        persistentVolumeClaim:
          claimName: navidrome-music

apiVersion: apps/v1
kind: Deployment
metadata:
  name: sftp-server
  namespace: media
  labels:
    app: sftp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sftp-server
  template:
    metadata:
      labels:
        app: sftp-server
    spec:
      securityContext:
        fsGroup: 1001
        runAsUser: 1001
        runAsGroup: 1001
      containers:
      - name: sftp-server
        image: atmoz/sftp:latest
        ports:
        - containerPort: 22
        envFrom:
        - secretRef:
            name: sftp-user-credentials
        volumeMounts:
        - name: sftp-volume-music
          mountPath: /home/olaf/music
      volumes:
      - name: sftp-volume-music
        persistentVolumeClaim:
          claimName: navidrome-music

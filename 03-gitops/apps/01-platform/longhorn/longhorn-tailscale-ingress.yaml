apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: longhorn-ui-tailscale
  namespace: longhorn-system
spec:
  ingressClassName: tailscale
  tls:
    - hosts:
        - longhorn
  defaultBackend:
    service:
      name: longhorn-frontend
      port:
        number: 80